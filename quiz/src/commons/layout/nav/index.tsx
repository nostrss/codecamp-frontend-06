import styled from '@emotion/styled'

const Wrapper = styled.div`
  height: 50px;
  background-color: orange;
`

export default function LayoutNav() {
  return <Wrapper>여기는 Navigation 영역입니다</Wrapper>
}
