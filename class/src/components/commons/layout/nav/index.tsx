import styled from '@emotion/styled';

const Wrapper = styled.div`
  height: 50px;
  background-color: lightgreen;
`;

export default function LayoutNav() {
  return <Wrapper>여기는 네비게이션 영역입니다</Wrapper>;
}
